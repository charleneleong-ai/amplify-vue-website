{
    "projectName": "amplify-vue-website",
    "version": "2.0",
    "frontend": "javascript",
    "javascript": {
        "framework": "vue",
        "config": {
            "SourceDir": "src",
            "DistributionDir": "dist",
            "BuildCommand": "yarn run build",
            "StartCommand": "yarn run serve"
        }
    },
    "providers": [
        "awscloudformation"
    ]
}